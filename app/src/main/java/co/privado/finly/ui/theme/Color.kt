package co.privado.finly.ui.theme

import androidx.compose.ui.graphics.Color

// ─────────────────────────────────────────────
//  Finly design-system tokens (finly-design-system.html)
//  Paleta: cuaderno de cuentas — tinta, latón, musgo, arcilla
// ─────────────────────────────────────────────

/** Fondo más profundo del teléfono / bordes del dispositivo */
val ColorInk = Color(0xFF12161B)

/** Superficie de las cards principales */
val ColorSurface = Color(0xFF1A2028)

/** Superficie elevada (split boxes, app icons, etc.) */
val ColorSurfaceHi = Color(0xFF212A34)

/** Contorno / separadores entre filas */
val ColorHair = Color(0xFF2A323C)

// ─── Acento principal — Latón (brass) ───────
/** Botones primarios, toggles activos, cifras destacadas */
val ColorBrass = Color(0xFFC9A667)

/** Variante opacada para fondos de iconos de menú */
val ColorBrassSoft = Color(0xFF8A7548)

/** Texto sobre brass (botón primario, toggle on) */
val ColorOnBrass = Color(0xFF1A1305)

// ─── Semánticos ─────────────────────────────
/** Ingresos */
val ColorMoss = Color(0xFF7FA07A)

/** Egresos / errores de negocio */
val ColorClay = Color(0xFFBE7B62)

// ─── Texto / UI neutral ──────────────────────
/** Texto principal, cifras grandes */
val ColorBone = Color(0xFFEEEAE1)

/** Texto secundario, labels, placeholders */
val ColorSlate = Color(0xFF8B93A0)

/** Error del sistema (contraseña incorrecta, red caída) */
val ColorError = Color(0xFFFF6B6B)
val ColorOnError = Color(0xFF1A0000)
